**Personal Digital Library App — Requirements**

Build a personal book cataloging web app (Progressive Web App) that works on Android and browser, stores data on Google Drive/cloud, and works offline.

**UI language:** Italian and English, user-switchable via a dropdown menu showing each option with its flag icon and language name (e.g. 🇮🇹 Italiano / 🇬🇧 English). All labels, buttons, and fixed field values (e.g. Cartaceo, Posseduto, Letto) translate with the selected UI language.

**App Layout:**
- **Top app bar:** hamburger menu icon (opens side menu), app title, search icon, sort icon, view-toggle icon.
- **Hamburger menu** (side drawer) contains: UI language switch, Import/Export, Google Drive connection/sync. Add a Show/Hide Stats toggle.
- **Main page** has two tabs: **Shelves** and **Books**. The selected tab's view is shown below.
- Both views share: a path/breadcrumb row, an item count, and a filter icon; each has its own sort and filter options.
- **Shelves view:** list of shelves, each showing a thumbnail icon, shelf name, book count, and a 3-dot menu (rename/delete/merge actions).
Clicking a shelf opens that shelf's Books view, filtered to just that shelf's books, updating the breadcrumb path (e.g. Home > Shelf name) and item count accordingly.
- **Books view:** books grouped by author surname under section labels, with an A–Z fast-scroll index on the side. Each row shows cover thumbnail, title, author, and status badges (reading status, format).
- **Add a book:** three methods — search online (Title/ISBN), scan ISBN via camera, or add manually. Reachable via a floating "+" button.

**Book fields** (in this exact order):
Title, Author, Publisher, Series, Volume, Language, Format, Reading Status, Ownership, Shelf, ISBN

- Each book belongs to **exactly one** shelf.
- Hidden fields (not shown in UI) store cover image data.
- Cover image is fetched automatically by ISBN, trying sources in this order: **Google Books → Open Library → Amazon (search by ISBN, then by Title if ISBN fails)**.
- **Note:** Amazon has no free public search API like Open Library/Google Books — using it requires an Amazon Associates account with the Product Advertising API, or another workaround. Flag this for the build chat when this step is reached.
- If no image is found from any source, show the placeholder icon.

**Reading Status:** dropdown options are Letto / In lettura / Da leggere only. On **import**, the values "Si", "Sì", "X" (case insensitive) are recognized as aliases for "Letto" — not shown as manual entry options.

**Ownership** options: Posseduto / In prestito / Non posseduto.

**Format** options: Cartaceo / Ebook / Audiolibro.

**Language** options: Italiano / English / Français / Deutsch / Español.

**Stats counters** (top of screen):
- Total books
- Read books (Reading Status = Letto, or Si/Sì/X on import)
- Owned books (Ownership = Posseduto)
- On loan books (Ownership = In prestito)
Stats counters section — note that visibility is controlled by this toggle (hidden by default state: visible, unless toggled off).

**Book list:**
- Search box (by Title/Author)
- Sort by Title, Author, or Reading Status
- Shelf filter chips at top of list
- Each book card shows badges for reading status, ownership, format, language, shelf

**Book management:**
- Add, edit, and delete books
- Add book modal: two tabs (search by Title / search by ISBN), searches Google Books API (then Open Library, then Amazon as fallback per above), shows cover/author/year, clicking a result pre-fills Title, Author, Publisher, ISBN, Language, and cover image if available; all fields remain manually editable after pre-fill
- ISBN barcode scanning via device camera as an additional way to add a book

**Shelf management:**
- Add and remove shelves dynamically
- Rename shelves inline by clicking the name
- Shelf names are case insensitive
- Renaming into a duplicate name triggers a confirmation dialog: "Sì, unisci" / "No, modifica nome", offering to merge the two shelves

**Import/Export:**
- Export to XLSX, columns in this exact order: Titolo, Autore, Editore, Serie, Volume, Lingua, Formato, Stato lettura, Posseduto, Scaffale, ISBN, Copertina (URL)
- Import from XLSX using either full set of headers below (not mixed):

| Field | Italian | English |
|---|---|---|
| Title | Titolo | Title |
| Author | Autore | Author |
| Publisher | Editore | Publisher |
| Series | Serie | Series |
| Volume | Volume | Volume |
| Language | Lingua | Language |
| Format | Formato | Format |
| Reading Status | Stato lettura | Reading Status |
| Ownership | Posseduto | Ownership |
| Shelf | Scaffale | Shelf |
| ISBN | ISBN | ISBN |
| Cover Image | Copertina | Cover Image |

- On import: validate columns; show a clear error listing any missing columns; mention any unrecognized columns
- Cover Image column is optional on import — if present, its URL is used; if absent/empty, the image is fetched via the ISBN fallback chain above
- If valid, show a success confirmation with book count and any new shelves to be created before importing
- New shelves found in import are added automatically
- If an imported book's ISBN already exists in the library, ask the user per duplicate whether to keep the existing entry or update it with the imported data
- If the import file contains **more than 5 duplicate ISBNs**, ask the user upfront whether to apply one **bulk decision** (keep existing / update with imported data) to all duplicates, or decide **per duplicate** as usual. If **5 or fewer** duplicates are found, always ask per duplicate.

**Google Drive Sync:**
- Sync is manual only (no auto-sync); triggered from the Google Drive option in the side drawer
- Sync always operates as a **full overwrite** — either local data replaces the Drive copy, or the Drive copy replaces local data. There is no per-book merge.
- If both sides changed since the last sync (conflict), a modal shows book counts and last-changed times for local vs. Drive, and asks the user to choose "Keep local" (overwrites Drive) or "Keep Drive" (overwrites local)
- If only one side changed, sync proceeds automatically without prompting

**UI/UX:**
- All confirmation dialogs use in-app modals (never browser confirm/alert)
- No limit on number of books
- Any icon/button that reveals additional UI (chips, dropdowns, panels) should toggle it closed on a second tap — never require a page refresh to dismiss.

**Technical Notes** *(implementation details — not user-facing behavior)*

**Google Books API**
- Endpoint: _(e.g. `https://www.googleapis.com/books/v1/volumes?q=...`)_
- Auth/API key: _(none needed for basic search, per current plan)_
- Quota limits: _(fair-use, no published hard cap as of research)_
- Fields mapped from response → app fields: _(e.g. `volumeInfo.title` → Title, `volumeInfo.imageLinks.thumbnail` → Cover Image)_
- Known quirks/edge cases: _(fill in as you hit them)_

**Future Enhancements / Backlog** *(not in current build scope)*

1. Extend cover auto-fetch to also trigger when editing an existing book and adding/changing its ISBN, if no cover is currently set.
2. When adding a book with an ISBN that already exists, offer the option to ignore the new entry or merge it with the existing one.
3. Bug/UX fix: while renaming a shelf, tapping/dragging the cursor on a touchscreen currently triggers the "open shelf → show books" action. This should be disabled while the rename input is active.
4. Add a new book field: **Genere** (Genre).
5. During book edit, allow entering the ISBN manually or scanning it via camera.
6. After entering/scanning an ISBN during book edit, offer to search for book data (Google Books, then Open Library as fallback); if a match is found, show the proposed updates (including cover image if available) and let the user choose whether to apply them.
7. During book edit, show the book's current cover; if missing, offer to fetch it via Google Books or Open Library, using the same search capability available when adding a book.
8. Drive sync has no automatic merge or timestamp-based auto-resolution — every conflict (both sides changed) requires a manual "Keep local" / "Keep Drive" choice from the user. Revisit if a smarter merge strategy becomes worth the added complexity.
9. Add a "mass change shelf" option: select multiple books and reassign them all to a different shelf in one action.
10. Add a Racks grouping layer, enabled via a toggle in the hamburger menu. Once activated: shelves can be assigned to a rack; one rack can contain multiple shelves; one shelf can belong to at most one rack. Books inherit their rack automatically through their shelf assignment (no separate book-level rack field).
11. Shelf management gets a "change rack" option per shelf, plus a mass-change option to reassign the rack for multiple shelves at once.
12. Add a third main tab, Racks, shown alongside Shelves and Books once the Racks feature is activated.
13.Racks can be created, renamed, and deleted.
14. Book list filtering extends to support filtering by rack, in addition to the existing shelf filter.
15. Add Spanish as an additional UI language option in the hamburger menu's language switcher.
16. Define and implement functionality for the top app bar's view-toggle icon (currently present in the layout with no behavior): toggles the Books list between Compact view (current row style — cover thumbnail, title, author, badges) and Detailed view (adds more per-book info visible inline, e.g. publisher, format, shelf, without opening the book).
19. Before executing any sync (regardless of direction — "Keep local" or "Keep Drive", or an automatic no-conflict sync), automatically trigger an XLSX export/download of the current local data as a safety backup, using the same export format already defined in Import/Export.
17App name & icon:
-App name (used for the home-screen icon / PWA install name, fixed regardless of UI language): My Library
-In-app title text (shown in the top app bar) translates with UI language: "My Library" (EN) / "La Mia Biblioteca" (IT)
18. App icon: to be redesigned to resemble a real library shelving rack (replacing the current placeholder icon)